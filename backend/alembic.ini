# Alembic configuration. The database URL is injected at runtime from
# `DATABASE_URL` (see migrations/env.py); the value here is a placeholder so
# `alembic` CLI commands still parse the file.
[alembic]
script_location = migrations
path_separator = os
prepend_sys_path = .
sqlalchemy.url = sqlite+aiosqlite:///./videolens-dev.sqlite3

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
